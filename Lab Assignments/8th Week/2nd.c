#include<stdio.h>
#define MAX_SIZE 1000000000

int queue[MAX_SIZE];
int front = -1, back = -1;



void enqueue(int n){
    
}

void max(int *arr, int n){

}

int main(){
    int n;
    scnaf("%d", &n);

    int arr[n];
    for(int i = 0; i < n; i ++)
        scanf("%d", &arr[i]);

    max(arr, n);
}